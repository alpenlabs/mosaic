//! CaC cryptographic procedures.

#[cfg(test)]
mod tests {
    #[test]
    fn test_dummy_please_remove() {
        // This has to be here otherwise nextest throws an error because there
        // was no tests to run.  Remove this as soon as we have an actual unit
        // test to run.
    }
}
