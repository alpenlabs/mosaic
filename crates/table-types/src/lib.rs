//! Mosaic high-level table data structures.

#![allow(
    missing_docs,
    reason = "these as standins that we'll define properly at a future time"
)]

// random standin defs that we'll define more concretely later

mod inputs;
mod outputs;

pub use inputs::*;
pub use outputs::*;
