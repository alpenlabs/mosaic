#![allow(
    missing_docs,
    reason = "these as standins that we'll define properly at a future time"
)]

// random standin defs that we'll define more concretely later
pub type TablesetCommitments = Vec<u8>;
pub type CacChoices = Vec<u8>;
pub type CacSeeds = Vec<u8>;
pub type TableExportMeta = Vec<u8>;
pub type TableEvalInputs = Vec<u8>;
pub type TableEvalOutputs = Vec<u8>;
