/// RPC server context trait.
///
/// Supplies information from the service's components for the RPC server.
pub trait RpcContextProvider: Sync + Send + 'static {
    // TODO add functions as we implement stuff
}
